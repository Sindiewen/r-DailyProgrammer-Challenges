#include <iostream>

using namespace std;

// Function prototypes
void crit(int Health, int die);     // Recursive function

int main()
{

    // Variables
    int enemyHealth;    // Stores the enemy health
    int dieRoll;        // Stores what the die rolled

    // Game Start:
    dieRoll = srand(static_cast<unsigned int>(rand(0)));    // Seeds a random number every second

}

void crit(int Health, int die)
{



}